# Previous application icons

This directory preserves the complete application-icon set that was active before the L4 light brand update on 2026-07-19.

The macOS menu-bar tray template is intentionally not duplicated here because it was not replaced. Its active path remains `../../tray-template.rgba`.
